# Copy to .env; fill in locally. Never put real tokens in this example.
COOLIFY_TOKEN=''
# Connector token copied from your existing Cloudflare Tunnel's Docker command.
TUNNEL_TOKEN=''
# Terraform reads TF_VAR_*; map the connector token automatically when sourced.
export TF_VAR_tunnel_token="$TUNNEL_TOKEN"
