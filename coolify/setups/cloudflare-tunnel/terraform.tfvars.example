coolify_endpoint    = "http://192.0.2.10:8000"
coolify_server_uuid = "REPLACE_WITH_EXISTING_SERVER_UUID"

# Optional Coolify settings:
# name              = "coolify-cloudflare-tunnel"
# cloudflared_image = "cloudflare/cloudflared:2026.9.1"

# Supply the connector token through .env / TF_VAR_tunnel_token, not this example.
